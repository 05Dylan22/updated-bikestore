import { createSlice } from "@reduxjs/toolkit";

export const iconSlice = createSlice({
  name: "handleIcons",
  initialState: {
    open: false,
    currentOpen: null,
    displayIconEl: null
  },
  reducers: {
    
  }
})